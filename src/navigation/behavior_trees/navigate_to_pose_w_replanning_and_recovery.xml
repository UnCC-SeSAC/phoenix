
<!--
  This Behavior Tree replans the global path periodically at 1 Hz and it also has
  recovery actions specific to planning / control as well as general system issues.
  This will be continuous if a kinematically valid planner is selected.

  RecoveryActions round-robin currently just alternates BackUpAndSpin / Spin.
  ClearingActions and Wait are commented out for now (test setup, re-enable later
  if needed). Standalone BackUp and the opposing Spin(-1.57) were dropped as
  redundant with BackUpAndSpin's own spin.

  FollowPath's inner RecoveryNode dropped from number_of_retries="1" to "0":
  that retry only cleared the local costmap and re-ran the identical path once
  before giving up outward, which just duplicated the RecoveryActions round-robin's
  first item. It cost a full extra movement_time_allowance cycle (~15s) before
  each round-robin slot could be reached. Frontier_explorer's external
  no-progress watchdog cannot see BT internals, so every second spent here was
  a second closer to it preempting the goal before recovery ever ran.
-->

<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <RateController hz="1.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="0" name="FollowPath">
          <FollowPath path="{path}" controller_id="FollowPath"/>
          <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>
      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <!--
          <Sequence name="ClearingActions">
            <ClearEntireCostmap name="ClearLocalCostmap-Subtree" service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" service_name="global_costmap/clear_entirely_global_costmap"/>
          </Sequence>
          -->
          <Sequence name="BackUpAndSpin">
            <BackUp backup_dist="0.20" backup_speed="0.1"/>
            <Spin spin_dist="1.57"/>
          </Sequence>
          <Spin spin_dist="1.57"/>
          <!-- <Wait wait_duration="5"/> -->
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
