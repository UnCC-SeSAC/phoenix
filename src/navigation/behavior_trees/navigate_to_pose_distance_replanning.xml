<?xml version="1.0"?>

<!--
  Replan after 0.30 m of odom-frame translation so in-place heading alignment
  does not continuously replace the path. Keep the existing recovery policy:
  enter the outer recovery immediately when FollowPath fails, then alternate
  BackUpAndSpin and Spin. Costmap clearing and Wait remain disabled.
-->
<root main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithDistanceReplanning">
        <DistanceController
          distance="0.30"
          global_frame="odom"
          robot_base_frame="base_footprint">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <ComputePathToPose goal="{goal}" path="{path}" planner_id="GridBased"/>
            <ClearEntireCostmap
              name="ClearGlobalCostmap-Context"
              service_name="global_costmap/clear_entirely_global_costmap"/>
          </RecoveryNode>
        </DistanceController>

        <RecoveryNode number_of_retries="0" name="FollowPath">
          <FollowPath path="{path}" controller_id="FollowPath"/>
          <ClearEntireCostmap
            name="ClearLocalCostmap-Context"
            service_name="local_costmap/clear_entirely_local_costmap"/>
        </RecoveryNode>
      </PipelineSequence>

      <ReactiveFallback name="RecoveryFallback">
        <GoalUpdated/>
        <RoundRobin name="RecoveryActions">
          <!--
          <Sequence name="ClearingActions">
            <ClearEntireCostmap
              name="ClearLocalCostmap-Subtree"
              service_name="local_costmap/clear_entirely_local_costmap"/>
            <ClearEntireCostmap
              name="ClearGlobalCostmap-Subtree"
              service_name="global_costmap/clear_entirely_global_costmap"/>
          </Sequence>
          -->
          <Sequence name="BackUpAndSpin">
            <BackUp backup_dist="0.20" backup_speed="0.1"/>
            <Spin spin_dist="1.57"/>
          </Sequence>
          <Spin spin_dist="1.57"/>
          <!-- <Wait wait_duration="5"/> -->
        </RoundRobin>
      </ReactiveFallback>
    </RecoveryNode>
  </BehaviorTree>
</root>
